# Current Slice: robust, low-impact firehose consumption + `skeet-prune` review/re-org

### Targets

#### Focus on longer-term maintainance

Refactor, review and minimisation of code for longer-term maintenance so "I can walk away from this for a while".

* the general expectation is that I want to be able to leave this repo for a while and go work on other stuff, and not need to worry about surprising code or lingering cruft/weirdness.
* split out code into sub-dirs based on role e.g. crates are at top-level in repo, and so should go into a subdir; follow generally accepted conventions where possible.

The general bias is to refactor towards patterns and structures that are the best practice for what kinds of things the `skeet-prune` crate is doing.

#### Firehose robustness, specifically

`skeet-prune` already consumes the firehose the efficient way — Jetstream (not the raw `com.atproto.sync.subscribeRepos` CBOR firehose) via `jetstream-oxide`, with `JetstreamCompression::Zstd`, a server-side `wantedCollections=app.bsky.feed.post` filter, and a shuffled four-endpoint list for connect-time failover. The transport is already lean; the gap is *consumption robustness*, not mechanism. Two failure-mode defects remain, both in `firehose.rs` / `firehose_stage.rs`:

- **Reconnects silently lose data.** `JetstreamConfig.cursor` is never set (we build the config with `..Default::default()`), so every reconnection starts in live-tail. A silent connection takes up to `recv_timeout` (30s) to notice, then up to ~5s × endpoints to re-establish — and every post in that whole window is gone, unobserved. On a redeploy or a flaky network this quietly punches holes in the stream we never see.
- **The reconnect loop has no backoff.** On connect failure `firehose_stage::run` does `warn; continue` with zero delay, and `max_retries: 0` disables the library's own retry — so during a real outage the DIY loop retry-storms all four public Jetstream instances as fast as it can. That's exactly the "unnecessary load on the central servers" we don't want to be a source of.

Goal: survive disconnects without losing observed posts, and stay a polite client even when things are on fire — without touching the (already-good) transport. The pipeline is already idempotent — the store is keyed by content hash / `SkeetId` — so replaying a few seconds of duplicate events on reconnect is safe and collapses to the same rows. That idempotency is the precondition that makes cursor-based resumption viable here.

Apply the cursor change first, then the backoff: backoff deliberately lengthens the disconnect gap, so the cursor (which makes gaps lossless) needs to be in place *before* we make gaps longer on purpose.

### Tasks

#### Group 0: longer-term maintainance changes

Each crate related to `skeet-prune` gets at least one full human pass (ideally): read all code, delete dead code, rework anything surprising, enforce the house rules along the way — `lib.rs` under 300 lines (extract modules if over), no `#[allow(dead_code)]`, no `unwrap`/`expect` in non-test code without a justified allow, and strip comment-rot (slice/phase/PR/task refs — see [[no-slice-phase-refs-in-code-comments]]). Note any non-obvious findings per crate. (Crate-specific NewType/error/visibility cleanups for `bluesky`/`shared` are listed in the *remaining-crates* 1.0 slice's Shared/support area; apply them wherever that crate's pass happens first.)

Background read: `skeet-prune-review.md` (stream-processing architecture review at repo root) — the deep read these tasks draw from; section refs (§N) point into it. Like the store pass, it is **not** a checklist — the items below are the cherry-picked subset; the rest is recorded as "noted, not pursued".

**Scope (decided — mirrors how the store slice narrowed to one crate):** the deep pass is **`skeet-prune` itself** (its 12 source modules + 6 bins + 3 integration tests). The detection libs (`face-detection`/`skin-detection`/`text-detection`), `shared`, `bluesky`, and `eval` each already have their own per-crate line in the *remaining-crates 1.0 slice* (`docs/next-slices.md`) — **don't double-book them here.** Two deliberate carve-outs from that rule, both because `skeet-prune`'s metadata stage is entangled with them: the `bluesky` metadata-filtering surface (`blocked_labels`/`fetch_post_thread`) and confirming the three detection models are still documented in `docs/`. Everything else in those crates stays in the remaining-crates slice.

**Hygiene pass result (already done in the review):** the house-rule sweep comes back **clean** — no comment-rot, no TODO/FIXME, no `#[allow(dead_code)]`, every `expect`/`unwrap` carries a justified allow, `lib.rs` is 14 lines, `text-detection/src/lib.rs` (419 lines, over the limit) belongs to the remaining-crates slice. So Group 0 is **shape work, not debt remediation**; the per-file table is in the review appendix. The tasks below are ordered smallest→safest first.

Tasks:
* [x] **Tighten the public surface — `pub mod` → `mod` + selective `pub use` (§7).** `skeet-prune` is a leaf binary crate nothing else imports, yet almost everything is `pub mod`. Drop the modules named by *nothing* external (`persistence`, `status`) to plain `mod`; for the rest, present a curated façade in `lib.rs` the way `classify`/`metrics` already do (`mod x; pub use x::{…}`), keeping exactly the surface the six bins + three tests use (`classify`, `firehose::SkeetCandidate`, `firehose_stage::run`, `prune_meta_stage::{run, check_metadata, MetaFilterOutcome}`, `prune_image_stage::run_workers`, `save_stage::run`, `pipeline::{…}`). Pure tidiness, no behaviour change. Verify `just clippy` + `just test-no-docker`.
* [ ] **Name the metrics boundary — kill the 12-positional-argument `emit` (§5). Highest value-per-effort; do early.** `PruneMetrics::emit(…)` takes 12 positional args (`metrics.rs:92`) and every test constructs them as bare positional literals. Introduce a single named snapshot struct (working name `PipelineSnapshot { firehose, meta, image, …depths…, posts, images, saved, rejections, categories, sole_categories }`), build it once in `Status::log_summary`, and pass `emit(&snapshot)`. Update the unit tests to construct the named value. Leave the cumulative→delta `prev_*` bookkeeping as-is (well-tested; rewriting to OTel-native deltas is noted-not-pursued). TDD-friendly: the existing `metrics.rs` tests already pin the delta behaviour — they should pass unchanged once re-expressed against the struct.
    * **Anticipate the *Make-statistics-visible* next-slice (don't make it harder).** That slice adds a `skeet-store` `Statistics` trait the pruner writes to **at this exact call site** (`Status::log_summary`, once per status interval) with **these exact numbers** — skeets seen (`posts`), images examined (`images`), images saved (`saved`) over an interval with start/end timestamps. So shape the snapshot to be reused by that future `Statistics::record`, not just by OTel: (a) **define `PipelineSnapshot` in a neutral module** (`pipeline.rs` or `status.rs`, **not** `metrics.rs`) so it carries no OTel dependency and the store write can consume the same value; (b) name the three content counts so the mapping to the future `Statistics` record is one-to-one (keep `posts`/`images`/`saved` or rename to `skeets_seen`/`images_examined`/`images_saved` — just don't bury them among the stage-throughput fields); (c) leave room to add the interval's start/end timestamps as a later field addition (`Status` already holds `started_at`/`last_log` to derive them) — don't lock the shape so that needs a reshape. **Do not** build the `Statistics` trait or the timestamps now — only avoid precluding them. Pairs with the §6 note ("don't re-plumb counting twice").
* [ ] **Unify shutdown on a `CancellationToken` (§2/§10) — preferred over a helper/newtype.** The `if tx.send(x).await.is_err() { warn!(…); return }` block is hand-copied ~6× (`firehose_stage.rs:38`, `prune_meta_stage.rs:62`/`:68`, `prune_image_stage.rs:110`/`:117`/`:123`) each with its own copy of the warn string. Collapse to one seam using a shared `tokio_util::sync::CancellationToken` (new workspace dep `tokio-util`): stages `select!` on the token alongside their send/recv, so closure-detection becomes one mechanism. **Chosen over** the lighter `forward()` helper / `PipelineSender<T>` newtype options (which only dedup the closed-channel case) because the token *also* gives **deliberate** shutdown — the groundwork the §10 drain-on-SIGTERM needs and the `is_err()` trick can't express. Scope-wise this lands the *seam* now (dedup + token plumbing); the full SIGTERM signal handler + `JoinSet` drain stays with Groups 2/3 (§10), which build on this token. Still **not** a control-flow rewrite (the `rust.md` `continue` guidance doesn't apply — these are early returns/`select!` arms). Verify `just clippy` + `just test-no-docker`.
* [ ] **Replace the `Arc<Mutex<mpsc::Receiver>>` image fan-out with `async-channel` MPMC (§3) — decided.** `prune_image_stage` wraps the receiver in `Arc<Mutex<…>>` (`:30`) so N workers can share it (`rx.lock().await.recv().await`, `:88`) — the classic "mpsc is single-consumer" workaround. Swap to `async-channel` (a real MPMC channel, new workspace dep): each worker holds a clone of the `Receiver`, the `Arc<Mutex>` and the lock-then-recv both disappear, and the **per-worker detector ownership stays intact** (each worker still owns its `FaceDetector`/`TextDetector`). **Chosen over** the `buffer_unordered` restructure (§9), which has no per-worker state and so would force the detectors to become shared `Arc` + `Sync` — a change reaching into `face-detection`/`text-detection`, not just plumbing. Behaviour-equivalent; verify throughput is unchanged with `just clippy` + `just test-no-docker` (the pipeline has no unit test today — a small one driving the fan-out would de-risk this; optional).
* [ ] **Move CPU-bound classification off the async worker thread (§4).** `classify_image` (face + skin + optional text inference, tens-to-hundreds of ms, no `await`) runs inline in the async task (`prune_image_stage.rs:99`), monopolising a runtime thread for its duration — fine at `--image-workers 2`, a starvation footgun if it's raised on a 4-vCPU node. Wrap the `classify_image` call in `tokio::task::spawn_blocking` (detectors are already moved into the worker, so `Send` holds; share via `Arc` if `spawn_blocking`'s `'static` bound requires it). This is the one genuine correctness-under-load item, not tidiness — keep the change minimal and confirm the worker still pulls the next item only after the blocking classify returns (preserving the bounded-concurrency contract). Verify `just clippy` + `just test-no-docker`.
* [ ] **Group the pipeline stages so the file tree shows the topology (§8).** The four ordered stages sit flat alongside plumbing/domain/telemetry, hiding the single most important fact about the crate (it's a pipeline). Mirror the `skeet-store` reorg precedent: move the four stages + the message-type plumbing into a `pipeline/` (or `stages/`) module, leaving `firehose` (ingestion), `classify` (domain), and `status`/`metrics` (telemetry) as siblings. **Earn the churn** — this crate is ~1/6th of `skeet-store`, so a single grouping is the whole win; do **not** over-fragment. Keep the `lib.rs` façade surface (post-task-1) identical. Verify `just clippy` + `just test-no-docker`. (Do this *after* the smaller code changes above so the moves are pure relocations.)
* [ ] **Document the `Post`-marker design in place (§6) — chosen over the `(Content, Counts)` redesign for this slice.** Counting rides the data plane: `MetaResult`/`ImageResult` each carry a `Post { image_count }` marker variant forwarded alongside data so the sink counts images-examined even when nothing survives (and the meta stage sends *twice* per candidate as a result). It's deliberate and deterministic but surprising — add a one-line doc-comment on the `Post` variants (`pipeline.rs`) explaining *why* telemetry travels with data (so a future reader doesn't "tidy" it away), respecting the `rust.md` rule that the comment describe the contract, not the task.
    * **Considered alternative (deferred, not lost — see review §6a):** have each stage emit a `(Content, Counts)` pair with `Counts` a monoid folded at the sink — the **Writer-monad carrier shape**, which would collapse the meta stage's double-send and lift the `Post` variant out of the content enums. Deferred because (a) it's more churn than a maintenance-slice doc note, (b) it's only the Writer's *shape* not the monad (this stream is infinite → a running fold/`scan` sampled per interval, **not** a `monadic`/`higher` Writer library), and (c) it **overlaps §5**: the folded `Counts` *is* the content-count half of `PipelineSnapshot` and of the future `Statistics` record, so if ever pursued it must be **co-designed with §5**, not added as a third counting scheme. TypeState on the content/count types is assessed-and-not-pursued (review §6a; consistent with the next-slices "ceremony exceeds payoff" note). **Don't re-plumb counting twice** across this, §5, and the *Make-statistics-visible* slice.
* [ ] **Entangled-dependency touch-ups (carve-outs only — not a full pass).** (a) `bluesky` metadata-filtering surface the meta stage leans on: `blocked_labels` returning `Vec<String>` → `Vec<Label>` is already owned by the remaining-crates slice, so here just confirm `prune_meta_stage`'s use of `blocked_labels`/`fetch_post_thread` reads cleanly and leave the type change to that slice (cross-reference, don't duplicate). (b) Confirm the three detection models (`face-detection`, `skin-detection`, `text-detection`) are each still documented in `docs/` (origin/purpose/rationale per the invariants) — file a doc fix if any drifted; the deep code pass of those crates stays in remaining-crates.
* [ ] **Close the pass.** Re-read the review's "noted, not pursued" items and confirm none have become worth doing; update `skeet-prune-review.md` only if a finding changed. Run `just clippy`, `just test-no-docker`, and `just mutants-on-diff` clean over the diff. (`just test` / `just validate-storage` Docker parts are a human/CI step.)

#### Group 1: bobby.houseofmoran.io should fall back to older data when newer unavailable

It's possible there could be an outage of the backing pruner process for > 48h which means what is shown on the feed or on website could expire gradually. I think what I'd like to do here is add a fallback mechanism where, for preferred Order + Limit, it will try successively older lists if it doesn't find anything in the preferred one. So, here's how it would roughly work:
* Finds all feeds available and orders them by age limit (i.e. 48h before 7d before 1y)
* When looking for a preferred feed (e.g. `quality-48h`) if that feed is empty, then it will find the next oldest available feed with the same ordering, in this case `quality-7d`. If that's also empty or missing then it should go to next.

This way when there is some sort of outage it gracefully degrades to older data.

**Decisions**
* **Home for the fallback: `skeet-publish`.** It already owns `FeedSource`/`PublishedImagesSource`/`RedisFeedSource`, the `PublishedList`/`PublishedListCatalog` keys, and `connect` — the fallback is one more source built from those. `skeet-feed` just wires it; `skeet-appraise` keeps its own `AvailableFeeds` (its dropdown/`resolve`/`UnknownFeed` policy differs and isn't a fallback).
* **Discovery is per-request, via the catalog** (the same shape `skeet-appraise` already uses). Re-reading the catalog + re-checking emptiness on each call is what makes degradation *live*: an outage that empties the preferred list mid-run, or a newly-published older list, is picked up without a restart. Cost is one extra `SMEMBERS` per request on the happy path.
* **Fallback chain = same `Order`, window ≥ preferred, ascending by `Limit::window()`.** "Next oldest with the same ordering" (doc) ⇒ stay within the preferred `Order` (e.g. `quality-48h` → `quality-7d` → `quality-4w` → `quality-1y`), never fall back to a *newer* window, never cross `Order` (quality→recency is out of scope). A missing preferred just means the chain starts at the next-oldest available list. "Empty or missing" is a single trigger because `PublishedList::read` already returns `[]` for both.

Tasks:
* [x] **Candidate-chain selection in `skeet-publish` (pure, tested first).** Add the logic that turns *(catalog specs + preferred `(Order, Limit)`)* into the ordered fallback chain: keep specs whose `Order` equals the preferred and whose `Limit::window()` ≥ the preferred's, sort ascending by window, yielding `[preferred-or-next-oldest, …, oldest]`. Keep it a pure function over `&[(Order, Limit)]` so it unit-tests without redis (examples + a proptest invariant: output is same-`Order`, non-decreasing window, and contains nothing newer than preferred). TDD: stub returning `vec![]`, write the ordering assertions (failing), then implement. (`Limit::window()` already exists; reuse it rather than re-deriving age.)
* [x] **`FallbackFeedSource` implementing both `FeedSource` and `PublishedImagesSource`.** New type in `skeet-publish` holding the publish redis url + the preferred `(Order, Limit)`. Per call: discover the catalog (`PublishedListCatalog::read` → `PublishedList::spec`), build the candidate chain (task above), then read each candidate (reusing `RedisFeedSource`) in order and return the **first non-empty** result — `FeedSkeleton` for `skeleton()`, `PublishedImages` for `published_images()` — carrying that list's `refreshed_at`. If every candidate is empty/missing, return the last (empty) result so the surface still renders. `examined_count()` is list-independent (single key) — delegate to the existing read unchanged. Unit-test first-non-empty selection against a fake reader (stub: always returns the preferred), then implement. Note: the happy path is one catalog read + one list read; only an outage walks further. (Decide whether to reuse `RedisFeedSource`'s transient-retry per candidate or read through one shared connection — default to reusing `RedisFeedSource` as-is for simplicity unless the per-candidate reconnect proves costly.)
* [x] **Wire `skeet-feed` onto the fallback sources.** In `skeet_feed.rs`, replace the two fixed `RedisFeedSource` constructions with `FallbackFeedSource`: Bluesky skeleton preferred `quality-48h`, website grid preferred `quality-4w`, both against `redis_publish_url`. Update the two explanatory comments (`skeet_feed.rs:77`/`:84`) to describe the preferred-with-fallback behaviour, not a fixed list. Behaviour must be byte-identical when the preferred list is populated. Verify `just clippy` + `just test-no-docker`.
* [x] **Integration test the degradation through the HTTP surface.** Using the existing redis-backed test infra (testcontainers, as `skeet-publish`/`skeet-appraise` tests do), assert via `skeet-feed`'s public HTTP endpoints: (a) with `quality-48h` empty/absent but `quality-7d` populated, `getFeedSkeleton` and the homepage serve the `quality-7d` data; (b) with `quality-48h` populated it wins; (c) the `examined_count` banner still renders regardless. Runs under `just test` (Docker) — Claude verifies the non-Docker parts with `just test-no-docker`; the container test is a human/CI step. Run `just mutants-on-diff` once the chain + source land.
  * Done in `crates/skeet-feed/tests/feed_integration.rs` (the subprocess-against-testcontainers infra). Caveat: the homepage grid's preferred is `quality-4w`, so its fallback chain can only widen (4w → 1y), never down to the narrower `quality-7d`. The test therefore exercises the homepage degrading `quality-4w` → `quality-1y` (and the feed `quality-48h` → `quality-7d`), which matches the actual wiring rather than the literal "homepage serves quality-7d" wording. mutants-on-diff: the pure chain + selection-walk mutants are caught by the unit tests; the redis-I/O delegations (`available_specs`/`examined_count`) are only caught under Docker (CI).

#### Group 2: cursor-based resumption

- Track `info.time_us` of the last processed event; set `JetstreamConfig.cursor` (a `DateTime<Utc>`) on reconnect, rewound ~5s for gapless playback. Currently `None` ⇒ every reconnect live-tails and drops the gap.
- Safe to replay because the store is idempotent (keyed by content hash / `SkeetId`).
- **Decision: in-memory only — no persistence (chosen).** The cursor lives in `firehose_stage::run`'s stack and survives reconnects *within a process*; on process restart it resets to `None` (live-tail) and the redeploy/crash gap is **accepted as lost**. This keeps the change small (a few lines, no new storage / startup-read / cursor-staleness concerns). Persisting it (to close the restart gap) is explicitly **out of scope** here — the review's §10 "drain on SIGTERM" is the *other* half of restart-loss and also stays deferred to its own work.

**API facts (verified against `jetstream-oxide 0.1.2`):** `JetstreamConfig.cursor: Option<chrono::DateTime<Utc>>`, default `None`, serialised on connect via `cursor.timestamp_micros()`; "absent or future cursor ⇒ live-tail". Every `JetstreamEvent` variant carries an `EventInfo { time_us: u64, .. }` (Commit `Create`/`Update`/`Delete`, `Identity`, `Account` — `time_us` is `#[serde(flatten)]`ed in), so the cursor can advance on *every* received event, not only on candidates. `chrono::DateTime::<Utc>::from_timestamp_micros(i64)` is the inverse used to rebuild the cursor.

Tasks:
* [ ] **Pure: extract `time_us` from any `JetstreamEvent` (tested first).** Add a small total helper `event_time_us(&JetstreamEvent) -> u64` in `firehose.rs` that matches all three variants (`Commit(Create|Update|Delete)`, `Identity`, `Account`) and returns `info.time_us`. Keep it pure so it unit-tests off captured/constructed events with no socket. TDD: stub returning `0`, write per-variant assertions (failing), then implement. (Advancing the cursor on every observed event — not just `app.bsky.feed.post` candidates — means a quiet period can't leave the cursor stale; the server filter already restricts the stream to posts anyway.)
* [ ] **Pure: build the rewound cursor from a `time_us` (tested first).** Add `cursor_from(time_us: u64) -> Option<DateTime<Utc>>` that rewinds by a named `RECONNECT_REWIND: Duration` const (~5s) and converts via `DateTime::from_timestamp_micros((time_us as i64).saturating_sub(REWIND_MICROS))`. Pure ⇒ proptest the invariants: output is ≤ the input instant, monotonic in `time_us`, and round-trips micros within the rewind. TDD: stub returning `None`, assert the rewind/floor behaviour (failing), then implement. The rewind buys boundary overlap; it's safe to replay because the sink is idempotent (the precondition stated above).
* [ ] **Thread a `cursor` param through `firehose::connect`.** Change `connect()` → `connect(cursor: Option<DateTime<Utc>>)` and set it on **every** `JetstreamConfig` built in the shuffled-endpoint loop (today each gets the implicit `None` from `..Default::default()`); on failover the *same* cursor applies to whichever endpoint wins. Update the sole caller (`firehose_stage::run`). No behaviour change when called with `None` (identical to today). Verify `just clippy` + `just test-no-docker`.
* [ ] **Track + feed the cursor across reconnects in `firehose_stage::run`.** Declare `let mut last_time_us: Option<u64> = None;` *outside* the outer reconnect loop (so it survives a reconnect but not a restart — the in-memory decision). First connect passes `None` (live-tail, nothing seen yet); each subsequent reconnect passes `cursor_from(last_time_us)`. In the inner recv loop, update `last_time_us = Some(event_time_us(&event))` immediately after `recv_async()` returns — *before* `extract_skeet_candidate` — so the cursor reflects everything observed, candidate or not. Verify `just clippy` + `just test-no-docker`. (Ordering: this lands **before** Group 3's backoff — backoff lengthens the gap on purpose, so the cursor that makes gaps lossless must already be in place. The `last_time_us` lives in the same loop the backoff state will join.)
* [ ] **Document the in-memory choice in place + close out.** One doc-comment at the `last_time_us` declaration stating the cursor is in-memory-only and a restart intentionally resumes at live-tail (contract, not task framing per `rust.md`). Then run `just clippy`, `just test-no-docker`, and `just mutants-on-diff` clean over the diff — the two pure helpers are the mutation-covered surface; the `run`-loop wiring is exercised against the live firehose (a human/CI smoke-check, since there's no pluggable stream until the replay slice).

#### Group 3: reconnect backoff

- Add exponential backoff with jitter between reconnect *cycles* (one cycle = one pass over all shuffled endpoints), capped ~30–60s; reset after a connection stays up.
- Currently `warn; continue` with no delay, and `max_retries: 0` disables the library's retry — so an outage retry-storms all four instances.
- Keep the DIY loop (it gives endpoint rotation, which `max_retries` doesn't); just add the delay.

Tasks:
* ...
