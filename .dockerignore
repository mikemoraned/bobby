# Build artifacts (same as .gitignore)
debug
target
**/*.rs.bk
*.pdb
**/mutants.out*/

# Runtime data (same as .gitignore)
store/
store*/
fallback-store/
models/
logs/

# Docker/deploy config (not needed inside the build)
fly*.toml
Dockerfile*
.dockerignore

# Dev/project files not needed for build
.git
docs/
Justfile
CLAUDE.md
*.env
